description: >
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    config-merge: rhems-japan/config-merge@x.y.z
  jobs:
    your-job:
      machine: true
      steps:
        - config-merge/config-check
  workflows:
    your-workflow:
      jobs:
        - your-job
